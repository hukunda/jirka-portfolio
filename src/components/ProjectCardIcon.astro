---
import { getProjectIconSvg } from '../lib/project-icons';

interface Props {
  slug: string;
}

const { slug } = Astro.props;
const iconSvg = getProjectIconSvg(slug);
---

<span class="project-card__icon" aria-hidden="true" set:html={iconSvg} />
